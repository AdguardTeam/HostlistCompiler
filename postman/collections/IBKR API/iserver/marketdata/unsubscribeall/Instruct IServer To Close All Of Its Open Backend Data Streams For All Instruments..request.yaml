$kind: http-request
description: Instruct IServer to close all of its open backend data streams for
  all instruments.
url: "{{baseUrl}}/iserver/marketdata/unsubscribeall"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Instruct IServer To Close All Of Its Open Backend Data
  Streams For All Instruments..resources/examples
order: 1000
