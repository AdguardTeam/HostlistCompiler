$kind: http-request
description: Retrieves a list of all sub-accounts and returns their net
  liquidity and available equity for advisors to make decisions on what accounts
  should be allocated and how.
url: "{{baseUrl}}/iserver/account/allocation/accounts"
method: GET
headers:
  Accept: application/json
examples: ./.resources/Allocatable Sub-Accounts.resources/examples
order: 1000
