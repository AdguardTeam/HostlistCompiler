$kind: http-request
description: Retrieves a list of all of the advisor’s allocation groups. This
  describes the name of the allocation group, number of subaccounts within the
  group, and the method in use for the group.
url: "{{baseUrl}}/iserver/account/allocation/group"
method: GET
headers:
  Accept: application/json
examples: ./.resources/List All Allocation Groups.resources/examples
order: 3000
