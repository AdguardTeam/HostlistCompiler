$kind: http-request
description: Get positions in accounts for a given instrument (no secDef await control)
url: https://api.ibkr.com/v1/api/portfolio/positions/:conid
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: conid
    value: "8314"
    description: "(Required) "
examples: ./.resources/Get Positions In Accounts For A Given
  Instrument.resources/examples
order: 1000
