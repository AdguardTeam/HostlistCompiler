$kind: http-request
description: "<br>**Scope**: `statements.read` OR `statements.write` OR
  `reports.write`<br>**Security Policy**: `Signed JWT`"
url: "{{baseUrl}}/gw/api/v1/statements"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
body:
  type: json
  content: |-
    {
      "accountId": "UXXXX",
      "startDate": "20230102",
      "endDate": "20230102",
      "mimeType": "text/html"
    }
examples: ./.resources/Generates Statements In Supported Formats Based On
  Request Parameters..resources/examples
order: 2000
