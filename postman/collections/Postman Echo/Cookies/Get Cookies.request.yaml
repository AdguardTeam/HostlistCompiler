$kind: http-request
description: Use this endpoint to get a list of all cookies that are stored with
  respect to this domain. Whatever key-value pairs that has been previously set
  by calling the "Set Cookies" endpoint, will be returned as response JSON.
url: https://echo.getpostman.com/cookies
method: GET
body:
  type: formdata
  content: []
scripts:
  - type: afterResponse
    code: >
      var responseJSON;

      try {
          tests["Body contains cookies"] = responseBody.has("cookies");
          responseJSON = JSON.parse(responseBody);
          tests["Cookies object is empty"] = (Object.keys(responseJSON.cookies).length === 0)
      }

      catch (e) { }


      tests["Status code is 200"] = responseCode.code === 200;
    language: text/javascript
examples: ./.resources/Get Cookies.resources/examples
order: 3000
