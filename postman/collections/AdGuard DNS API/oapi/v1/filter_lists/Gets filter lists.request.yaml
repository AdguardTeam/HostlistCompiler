$kind: http-request
description: >-
  The `GET /oapi/v1/filter_lists` endpoint retrieves a list of filter lists. The
  response for this request can be represented as a JSON schema as follows:


  ``` json

  {
    "type": "array",
    "items": {
      "type": "object",
      "properties": {
        "categories": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "category": { "type": "string" },
              "description": { "type": "string" },
              "value": { "type": "string" }
            }
          }
        },
        "description": { "type": "string" },
        "download_url": { "type": "string" },
        "filter_id": { "type": "string" },
        "homepage_url": { "type": "string" },
        "name": { "type": "string" },
        "rules_count": { "type": "string" },
        "source_url": { "type": "string" },
        "tags": {
          "type": "array",
          "items": { "type": "string" }
        },
        "time_updated": { "type": "string" }
      }
    }
  }

   ```
url: "{{baseUrl}}/oapi/v1/filter_lists"
method: GET
headers:
  Accept: "*/*"
examples: ./.resources/Gets filter lists.resources/examples
order: 1000
