$kind: http-request
description: Delete the `toAccount` account to clean up after this test case.
url: "{{baseUrl}}/api/v1/accounts/:accountId"
method: DELETE
headers:
  Content-Type: application/json
pathVariables:
  accountId: "{{e2e_user_account_id_to}}"
body:
  type: text
  content: "{}"
scripts:
  - type: afterResponse
    code: //this request inherits the status check at the folder level.
    language: text/javascript
examples: ./.resources/[Teardown][10] Delete The User Account -
  ToAccount.resources/examples
order: 10000
