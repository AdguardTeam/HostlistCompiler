$kind: http-request
description: Deletes all expired entries from PostgreSQL storage and cache tables
url: http://{{baseUrl}}/admin/pg/clear-expired
method: POST
headers:
  Accept: application/json
body:
  type: text
  content: ""
auth:
  type: apikey
  credentials:
    key: X-Admin-Key
    value: "{{apiKey}}"
    in: header
examples: ./.resources/Clear expired PostgreSQL entries.resources/examples
order: 1000
