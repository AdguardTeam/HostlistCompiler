$kind: http-request
description: |-
  ### Get Site Devices

  This endpoint retrieves the devices associated with a specific site.

  #### Request

  - Method: GET
      
  - URL: `{{https}}{{local-base-url}}/{{proxy}}/{{network}}/{{integration}}/{{version}}/{{sites}}/{{site-id}}/{{devices}}`
      

  #### Response

  The response for this request is a JSON schema, which provides the structure and data types of the response body. It includes the properties and their respective types that can be expected in the response payload.
url: "{{https}}{{local-base-url}}/{{proxy}}/{{network}}/{{integration}}/{{versi\
  on}}/{{sites}}/{{site-id}}/{{devices}}"
method: GET
order: 4000
