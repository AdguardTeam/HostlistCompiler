/**
 * @module filters
 * Rule filtering utilities for the AdBlock Compiler.
 *
 * Exports {@link RuleFilter}, which applies inclusion/exclusion criteria to a
 * collection of rule strings as part of the compilation pipeline.
 */
export { RuleFilter } from './RuleFilter.ts';
